# Agent-0 Dependencies

# API Server
flask>=3.0
flask-cors>=4.0

# File Watching
watchfiles>=0.21

# LLM Providers
anthropic>=0.40
openai>=1.50
google-generativeai>=0.8

# Database (vector search extension for SQLite)
sqlite-vec>=0.1

# Desktop UI handled by Tauri (Rust) — no Python UI dependencies needed

# Build
pyinstaller>=6.0
